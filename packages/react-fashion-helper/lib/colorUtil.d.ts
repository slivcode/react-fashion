/**
 * generate hsl, s, l < 1 and automatically convert to percent
 * @param h
 * @param s
 * @param l
 * @param a
 * @returns {string}
 */
export declare const hslaStr: (h: number, s: number, l: number, a?: number) => string;
export declare const rgbaStr: (x: any, y: any, z: any, a?: any) => string;
